# Public contact email shown/used on the site. Leave blank until a real
# business email exists -- do not publish a placeholder/fake address.
NEXT_PUBLIC_CONTACT_EMAIL=

# Optional: enables the contact form server action to actually send email.
# Sign up at https://resend.com, add a verified sending domain, then set:
RESEND_API_KEY=
